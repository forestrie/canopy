// Playwright configuration - placeholder for future e2e tests
// This will be configured when the frontend application is added
import { defineConfig } from "@playwright/test";

export default defineConfig({
  testDir: "./tests/e2e",
  // Minimal config - will be expanded when e2e tests are added
});
