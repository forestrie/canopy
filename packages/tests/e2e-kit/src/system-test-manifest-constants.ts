/**
 * Shared manifest sentinel values for system-testing lane YAML.
 * Keep in sync with `system-testing/manifests/lane-a.example.yaml`.
 */

/** KS256 Imutable placeholder until Lane A pins a real KS256 contract. */
export const KS256_UNIVOCITY_MANIFEST_PLACEHOLDER =
  "0x0000000000000000000000000000000000000002";
